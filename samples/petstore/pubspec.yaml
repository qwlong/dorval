name: petstore_sample
tdescription: Petstore API client generated by Dorval - A Dart/Flutter code generator for OpenAPI
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # HTTP Client
  dio: ^5.3.2
  
  # Data models (Freezed)
  freezed_annotation: ^3.0.0
  json_annotation: ^4.8.1
  
  # Utilities
  equatable: ^2.0.5
  collection: ^1.18.0
  
  # For FormData support
  http_parser: ^4.0.2

dev_dependencies:
  # Code generation
  build_runner: ^2.4.6
  freezed: ^3.0.0
  json_serializable: ^6.7.1
  
  # Testing
  test: ^1.24.0
  dio_smart_retry: ^5.0.0
  
  # Linting
  lints: ^3.0.0
  very_good_analysis: ^5.1.0